#include <bits/stdc++.h>
using namespace std;

void optimalPage(int pages[], int n, int frame){

}

int main()
{
	int pages[] = { 3, 2, 1, 2, 3, 6, 4, 7, 2, 3, 4, 2, 1, 5, 7, 6, 2, 6, 2, 2, 3, 3 };
	int page_size = sizeof(pages) / sizeof(pages[0]);
	int frames = 3;
	optimalPage(pages, page_size, frames);
	return 0;
}